<h2>販売店・飲食店一覧</h2>
<div class="search_form col-3">
  <%= form_with url: brewery_shops_path, local: true, method: :get do |f| %>
    <%= f.text_field :search, class: "form-control", placeholder: "酒造検索" %>
    <%= f.submit "検索", class: "btn btn-light" %>
  <% end %>
</div>
<%= render 'index', users: @users %>