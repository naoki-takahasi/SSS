<div class="container">
  <div class="row mt-3">
    <div class='col-md-4'>
      <%= render 'sake', sake: @sake %>
      <%= link_to '編集', edit_brewery_sake_path(@sake), class: "btn btn-outline-secondary" %>
    </div>
    <div class="col-md-7 offset-md-1">
      <div class="row">
        <h4>取扱店一覧</h4>
        <%= render 'brewery/shops/index', users: @favorite_shops %>
      </div>
      <div class="row">
        <h4>コメント一覧</h4>
        <%= render 'brewery/comments/comment_list', comments: @comments %>
      </div>
    </div>
  </div>
</div>