<div class="container">
  <div class="row mt-3">
    <div class='col-md-6'>
      <h2>フォローした販売店・飲食店</h2>
      <% if @users.blank? == false %>
        <%= render 'brewery/shops/index', users: @users %>
      <% else %>
        <p>フォローしたお店がありません。</p>
      <% end %>
    </div>
    <div class='col-md-6'>
      <h2>お取り扱いの販売店・飲食店</h2>
      <% if @shops.blank? == false %>
        <%= render 'brewery/shops/index', users: @shops %>
      <% else %>
        <p>お取り扱いのお店はございません。</p>
      <% end %>
    </div>
  </div>
</div>
