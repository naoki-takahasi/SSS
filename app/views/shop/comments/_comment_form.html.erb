<%= form_with model: [sake, comment], url: shop_sake_comments_path(sake.id), local: false do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, class: 'form-control book_body' %>
  </div>
  <div class="form-group">
    <%= f.submit class: 'btn btn-success' %>
  </div>
<% end %>