<table>
  <% comments.zip(shops).each do |comment, shop| %>
    <tr>
      <td>
        <%= link_to admin_shop_path(comment.shop_id) do %>
          <%= image_tag shop.get_image, size: "100x100" %><br>
          <%= shop.name %>
        <% end %>
      </td>
      <td><%= comment.comment %></td>
      <td>
        <%= link_to 'Destroy', admin_sake_comment_path(sake, comment), method: :delete, remote: true, data: { confirm: '本当に消しますか？' }, class: "btn btn-sm btn-danger"%>
      </td>
    </tr>
  <% end %>
</table>