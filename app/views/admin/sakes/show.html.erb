<div class="container">
  <div class="row mt-3">
    <div class='col-md-4'>
      <%= render 'brewery/sakes/sake', sake: @sake %>
      <%= link_to '編集', edit_admin_sake_path(@sake), class: "btn btn-outline-secondary" %>
    </div>
    <div class="col-md-7 offset-md-1">
      <h4>コメント一覧</h4>
      <%= render 'admin/comments/comment_list', comments: @comments, sake: @sake %>
    </div>
  </div>
</div>